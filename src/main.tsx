import React from "react";
import ReactDOM from "react-dom/client";
import SparktalkApp from "./SparktalkApp.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SparktalkApp />
  </React.StrictMode>
);
